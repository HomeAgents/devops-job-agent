# Copy to ~/orchestrator-data/users/<safe>/linkedin.env (chmod 600). Admin-only.
# Enables unattended LinkedIn refresh on the home worker Mac for this subscriber.
LINKEDIN_EMAIL=user@example.com
LINKEDIN_PASSWORD=app-password-or-account-password
